import { Node, Tree } from './types';

// Tree Operations
export function addNode(tree: Tree, node: Node, parent: number | null) {}

export function removeNode(tree: Tree, node: Node, parent: number | null) {}

// Algorithm for Adding node

// Algorithm for Removing node
